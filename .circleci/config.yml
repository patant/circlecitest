version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: mkdir
          command: mkdir patrik
    docker:
      - image: ubuntu
    steps:
      - run:
          name: ls
          command: ls -l
      