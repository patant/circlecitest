version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: mkdir
          command: mkdir patrik
  verify:
    docker:
      - image: ubuntu
    steps:
      - check
      - run:
          name: ls
          command: ls -l
      